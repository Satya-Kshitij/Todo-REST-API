# Todo-REST-API
A lightweight RESTful API for managing todos, built with Node.js and Express. Supports full CRUD operations, input validation, and centralized error handling — ready to run out of the box with an in-memory data store.
